<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<ScrollPane fitToWidth="true" prefHeight="1000" prefWidth="1200"
            styleClass="dashboard-scroll"
            stylesheets="@../../../../css/application.css"
            xmlns="http://javafx.com/javafx/17.0.2-ea"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.example.csit228capstone.controller.alerts.AlertsController">

    <content>
        <VBox fillWidth="true" spacing="16">
            <padding><Insets bottom="16" left="16" right="16" top="16"/></padding>

            <!-- Header -->
            <HBox alignment="CENTER_LEFT" spacing="12">
                <Label styleClass="page-title" text="Broadcasts"/>
                <Region HBox.hgrow="ALWAYS"/>
                <Button onAction="#openCreateBroadcast"
                        styleClass="quick-action-btn, qa-warning"
                        text="Create Broadcast"/>
            </HBox>

            <!-- KPI Cards -->
            <HBox spacing="16">
                <VBox prefHeight="106" prefWidth="110"
                      styleClass="kpi-card, kpi-info" HBox.hgrow="ALWAYS">
                    <Label styleClass="kpi-label" text="Broadcasts"/>
                    <Label fx:id="lblBroadcasts" styleClass="kpi-value" text="0"/>
                </VBox>
                <VBox prefHeight="106" prefWidth="110"
                      styleClass="kpi-card, kpi-warning" HBox.hgrow="ALWAYS">
                    <Label styleClass="kpi-label" text="Scheduled"/>
                    <Label fx:id="lblScheduled" styleClass="kpi-value" text="0"/>
                </VBox>
                <Region prefWidth="200" HBox.hgrow="ALWAYS"/>
            </HBox>

            <!-- Table -->
            <HBox spacing="16" VBox.vgrow="ALWAYS">
                <VBox spacing="12" styleClass="panel-card" HBox.hgrow="ALWAYS">
                    <Label styleClass="panel-title" text="Recent Alerts &amp; Broadcasts"/>

                    <TableView fx:id="alertsTable" prefHeight="400"
                               styleClass="incident-table" VBox.vgrow="ALWAYS">
                        <columns>
                            <TableColumn fx:id="colType"     text="Type"      prefWidth="140"/>
                            <TableColumn fx:id="colPriority" text="Priority"  prefWidth="90"/>
                            <TableColumn fx:id="colMessage"  text="Message"   prefWidth="260"/>
                            <TableColumn fx:id="colSentBy"   text="Sent By"   prefWidth="120"/>
                            <TableColumn fx:id="colSentDate" text="Date Sent" prefWidth="140"/>
                            <TableColumn fx:id="colAction"   text="Actions"   prefWidth="140"
                                         sortable="false"/>
                        </columns>
                        <columnResizePolicy>
                            <TableView fx:constant="CONSTRAINED_RESIZE_POLICY"/>
                        </columnResizePolicy>
                    </TableView>
                </VBox>
            </HBox>

        </VBox>
    </content>
</ScrollPane>
